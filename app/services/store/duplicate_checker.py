from __future__ import annotations

# Duplicate detection is now handled directly by PostgresStore.has_active_chunks().
# This module is kept for backwards compatibility but is no longer used internally.
