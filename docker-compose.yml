services:
  vector-db:
    build: .
    container_name: alumnx-vector-db
    ports:
      - "8001:8000"
    volumes:
      - ./vector_store:/app/vector_store
    env_file:
      - .env
    # Required env vars: GOOGLE_API_KEY, POSTGRES_URL
    restart: unless-stopped
